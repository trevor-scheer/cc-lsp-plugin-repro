# cc-lsp-plugin-repro

Minimal reproduction repo for testing Claude Code with LSP plugins.

## LSP Plugins

This repo uses two LSP plugins:
- TypeScript language server (via tsconfig.json)
- GraphQL language server (via .graphqlrc.yml)
